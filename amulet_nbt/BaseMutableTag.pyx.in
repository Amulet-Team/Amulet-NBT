{{py:from tools.tempita import include}}
{{include("amulet_nbt/BaseTag.pyx.in", cls_name=cls_name)}}

    @property
    def value({{cls_name}} self):
        """
        A copy of the data stored in the class.
        Use the public API to modify the data within the class.
        """
        return copy(self.value_)

    __hash__ = None
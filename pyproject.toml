[build-system]
requires = [
    "setuptools>=42",
    "versioneer",
    "packaging",
]
build-backend = "build_requires"
backend-path = [""]

[project]
name = "amulet-nbt"
authors = [
    {name = "James Clare"},
    {name = "Ben Gothard"},
]
description = "Read and write Minecraft NBT and SNBT data."
dynamic = ["version", "readme", "dependencies"]
requires-python = ">=3.11"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]

[project.optional-dependencies]
docs = [
    "Sphinx>=1.7.4",
    "sphinx-autodoc-typehints>=1.3.0",
    "sphinx_rtd_theme>=0.3.1",
]
dev = [
    "setuptools>=42",
    "versioneer",
    "packaging",
    "wheel",
    "amulet_pybind11_extensions~=1.0",
    "pybind11_stubgen>=2.5.4",
    "black>=22.3",
    "isort",
    "autoflake",
    "mypy",
    "types-pyinstaller",
]

[project.urls]
Homepage = "https://www.amuletmc.com"
Repository = "https://github.com/Amulet-Team/Amulet-NBT"
Issues = "https://github.com/Amulet-Team/Amulet-NBT/issues"

[tool.setuptools]
include-package-data = false

[tool.setuptools.package-data]
"*" = [
    "*Config.cmake",
    "**/*.hpp",
    "**/*.dll",
    "**/*.so",
    "**/*.dylib",
    "**/*.lib",
]

[tool.setuptools.dynamic]
readme = {file = ["README.md"], content-type = "text/markdown"}

[project.entry-points.pyinstaller40]
hook-dirs = "amulet.nbt.__pyinstaller:get_hook_dirs"

[tool.versioneer]
VCS = "git"
style = "pep440"
versionfile_source = "src/amulet/nbt/_version.py"
versionfile_build = "amulet/nbt/_version.py"
tag_prefix = ""
parentdir_prefix = "amulet_nbt-"

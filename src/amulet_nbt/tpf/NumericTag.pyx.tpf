{{py:from template import include}}
{{include("amulet_nbt/tpf/ImmutableTag.pyx.tpf", **locals())}}

    def __repr__({{py_cls}} self):
        return f"{{py_cls}}({self.cpp})"

    def __str__({{py_cls}} self):
        return str(self.cpp)

    def __reduce__({{py_cls}} self):
        return {{py_cls}}, (self.cpp,)

    def __copy__({{py_cls}} self):
        return {{py_cls}}.wrap(self.cpp)

    def __deepcopy__({{py_cls}} self, memo=None):
        return {{py_cls}}.wrap(self.cpp)

    def __hash__({{py_cls}} self):
        return hash(({{tag_id}}, self.cpp))

    def __int__({{py_cls}} self):
        return int(self.cpp)

    def __float__({{py_cls}} self):
        return float(self.cpp)

    def __bool__({{py_cls}} self):
        return bool(self.cpp)

{{include("amulet_nbt/tpf/Comparison.pyx.tpf", **locals())}}